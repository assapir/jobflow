# Local development overrides
# This file is loaded BEFORE .env, so values here take precedence

# Dev Auth Bypass - enables login without LinkedIn credentials
DEV_AUTH_BYPASS=true

# Use test JWT secrets for local dev
JWT_SECRET=local_dev_jwt_secret_not_for_production
JWT_REFRESH_SECRET=local_dev_refresh_secret_not_for_production
